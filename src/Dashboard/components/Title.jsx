import React from "react";

const Title = ({ name }) => {
  return (
    <span class="s">
      <span class="title">
        <span class="o">{name}</span>
        <span class="c1"></span>
        <span class="c2"></span>
      </span>
    </span>
  );
};

export default Title;
